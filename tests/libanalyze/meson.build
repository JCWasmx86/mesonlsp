test(
    'optiondiagnosticstest',
    executable(
        'optiondiagnosticstest',
        'optiondiagnostics.cpp',
        dependencies: [
            analyze_dep,
            gtest_dep,
        ] + extra_deps + extra_libs,
    ),
    protocol: 'gtest',
)
